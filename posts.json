---
---
{
  "posts": [
  {% for post in site.posts %}
  {
    "title": "{{post.title}}",
    "tags": [
      {% for tag in post.tags %}
        "{{tag}}"{% if forloop.index != forloop.length %},{% endif %}
      {% endfor %}
    ]
  }
  {% endfor %}
  ]
}